import React from 'react';

import PropTypes from 'prop-types';

const LatestMoviesPage = () => <div>LatestMovies</div>;

LatestMoviesPage.propTypes = {};

export default React.memo(LatestMoviesPage);
